# Billing Platform - Smart Subscription & Billing Management System

A premium, responsive SaaS frontend interface built for a **Smart Subscription & Billing Management System**. Designed in the aesthetic styles of modern platforms like Vercel, Linear, Stripe, and Clerk, featuring custom glassmorphic styling, React, TypeScript, Tailwind CSS, Framer Motion, and seamless dark/light mode customization.

## 🚀 Quick Start (Running Locally)

### Option 1: Vite Dev Server (Recommended)
Open your shell inside the `frontend` folder and execute:
```bash
npm run dev
```
Then, point your browser to `http://localhost:3000`.

### Option 2: Production Build & Preview
```bash
npm run build
npm run preview
```

---

## 🔑 Demo Credentials

- **Admin Account**:
  - **Email:** `admin@billingplatform.com`
  - **Password:** `Admin@123`
  - **Role:** Admin (Access to `/admin/dashboard`, customer directory, pricing plans, financial analytics, invoice creation)

- **Customer Registration**:
  - Self-register on the Login page (`/register`) to test Customer access (`/customer/dashboard`, active subscription, PDF invoices).

---

## 🎨 Technology Stack

- **Core Framework:** React 18 & TypeScript
- **Bundler:** Vite
- **Styling & Theme:** Tailwind CSS with CSS Variables (`[data-theme="dark"]`, `[data-theme="light"]`)
- **Iconography:** Lucide React Icons
- **Animations:** Framer Motion
